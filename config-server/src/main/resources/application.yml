server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/thisischetanyadav/kode-ecom-microservices.git
          default-label: main
          clone-on-start: true 
          search-paths: configuration